@model PharmacyNetwork.ApplicationCore.Entities.ProductCategory

@{
    ViewData["Title"] = "Изменение данных о категории";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mb-3 text-center">Изменение данных о фирме</h1>

<h4 class="mb-3">Введите новые данные о фирме</h4>

<div class="row justify-content-center">
    <div class="col-md-6">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CategId" />
            <div class="form-group">
                <label asp-for="CategName" class="control-label"></label>
                <input asp-for="CategName" class="form-control" />
                <span asp-validation-for="CategName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CategMarkup" class="control-label"></label>
                <input asp-for="CategMarkup" class="form-control" />
                <span asp-validation-for="CategMarkup" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Изменить" class="btn btn-primary btn-lg" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="btn btn-secondary btn-lg">Вернуться</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
